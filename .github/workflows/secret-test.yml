name: Deploy to GitHub Pages
on:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Create env file with encoded API key
        run: |
          # API 키를 Base64로 인코딩
          ENCODED_API_KEY=$(echo -n "${{ secrets.API }}" | base64)
          # JavaScript 파일 생성 (더 안정적인 버전)
          cat > env.js << EOL
          window.env = window.env || {};
          (function(){
            try {
              var _0x${RANDOM}=atob('${ENCODED_API_KEY}');
              Object.defineProperty(window.env, 'API', {
                get: function() { return _0x${RANDOM}; },
                configurable: false,
                enumerable: false
              });
            } catch(e) {
              console.error('API 키 초기화 중 오류가 발생했습니다:', e);
            }
          })();
          EOL

      - name: Setup Pages
        uses: actions/configure-pages@v3

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
